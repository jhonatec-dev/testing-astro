---
import dayjs from "dayjs";

const { frontmatter } = Astro.props;
---
<h1>{frontmatter.title}</h1>
<p>Published on: {dayjs(frontmatter.pubDate).tz('America/Sao_Paulo').format("YYYY-MM-DD")}</p>
<p>Written by {frontmatter.author}</p>
<slot />
<div>
  Footer
</div>